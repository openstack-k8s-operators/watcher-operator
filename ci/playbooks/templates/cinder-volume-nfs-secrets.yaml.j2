---
apiVersion: v1
kind: Secret
metadata:
  labels:
    service: cinder
    component: cinder-volume
  name: {{ name }}
type: Opaque
stringData:
  {{ name }}: |
    [{{ backend_name }}]
    nas_host={{ cifmw_nfs_ip }}
    nas_share_path={{ '/data/' + share }}
