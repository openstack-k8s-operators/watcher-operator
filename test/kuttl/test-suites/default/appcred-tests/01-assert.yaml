apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 600
commands:
  - script: |-
      set -euo pipefail
      NS="${NAMESPACE}"
      oc wait -n "${NS}" watcher watcher-kuttl --for=condition=Ready --timeout=600s
